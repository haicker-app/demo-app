<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vulnerable Notes App</title>
    <link rel="stylesheet" href="/static?file=style.css">
</head>
<body>
    <div class="container">
        <div class="nav">
            <a href="/">Home</a>
            <a href="/public-notes">Public Notes</a>
            <% if (user) { %>
                <a href="/dashboard">Dashboard</a>
                <a href="/create-note">Create Note</a>
                <a href="/backup">Backup</a>
                <a href="/logout">Logout</a>
            <% } else { %>
                <a href="/login">Login</a>
                <a href="/register">Register</a>
            <% } %>
        </div>
        
        <h1>Welcome to Vulnerable Notes App</h1>
        <p>A note-taking application with intentional security vulnerabilities for educational purposes.</p>
        
        <% if (user) { %>
            <p>Welcome back! <a href="/dashboard" class="btn">Go to Dashboard</a></p>
        <% } else { %>
            <p>Please <a href="/login" class="btn">Login</a> or <a href="/register" class="btn">Register</a> to get started.</p>
        <% } %>
    </div>
</body>
</html> 