<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Public Notes - Vulnerable Notes App</title>
    <link rel="stylesheet" href="/static?file=style.css">
</head>
<body>
    <div class="container">
        <div class="nav">
            <a href="/">Home</a>
            <a href="/public-notes">Public Notes</a>
            <a href="/login">Login</a>
            <a href="/register">Register</a>
        </div>
        
        <h1>Public Notes</h1>
        <p>Browse notes shared publicly by our community.</p>
        
        <div style="margin-bottom: 20px;">
            <form method="GET" action="/public-notes" class="search-form">
                <input type="text" name="search" value="<%= search %>" placeholder="Search notes...">
                <button type="submit" class="btn">Search</button>
                <% if (search) { %>
                    <a href="/public-notes" class="btn" style="background: #6c757d;">Clear</a>
                <% } %>
            </form>
        </div>
        
        <% if (notes.length === 0) { %>
            <p>No public notes available yet.</p>
        <% } else { %>
            <% notes.forEach(function(note) { %>
                <div class="note-card">
                    <div class="note-title"><%= note.title %></div>
                    <div class="note-meta">
                        By: <%= note.author %> | 
                        Created: <%= new Date(note.created_at).toLocaleDateString() %>
                    </div>
                    <div class="note-content">
                        <%= note.content.substring(0, 200) %>...
                    </div>
                </div>
            <% }); %>
        <% } %>
        
        <div style="margin-top: 20px;">
            <a href="/login" class="btn">Login to Create Notes</a>
        </div>
    </div>
</body>
</html> 